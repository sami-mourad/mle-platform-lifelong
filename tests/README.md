# Test taxonomy

- `unit`: pure logic and local side effects;
- `contract`: external request/artifact contracts;
- `integration`: model training and multi-component behavior;
- future `failure`: container/service disruption drills;
- future `load`: latency and saturation tests.
