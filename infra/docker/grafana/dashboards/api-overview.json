{
  "annotations": {
    "list": []
  },
  "editable": true,
  "panels": [
    {
      "type": "timeseries",
      "title": "Prediction rate by model source",
      "targets": [
        {
          "expr": "sum(rate(mle_predictions_total[5m])) by (model_source)",
          "legendFormat": "{{model_source}}"
        }
      ],
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 0
      }
    },
    {
      "type": "timeseries",
      "title": "P95 inference latency",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(mle_prediction_latency_seconds_bucket[5m])) by (le))",
          "legendFormat": "p95"
        }
      ],
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 0
      }
    }
  ],
  "schemaVersion": 41,
  "title": "MLE Platform API Overview",
  "version": 1
}